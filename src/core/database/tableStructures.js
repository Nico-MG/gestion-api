const iProduct = {
	idp: ["product_id", "string"],
	nombre: ["name", "string"],
	cat: ["type", "string"],
	cit: ["quantity", "number"],
	mCit: ["min_quantity", "number"],
	precio: ["price", "number"],
};

const tables = {
	products: iProduct,
};

export default tables;
